import React from "react";

const CreateCoefBlock = coef => {
  return <React.Fragment>{console.log(coef)}</React.Fragment>;
};
export default CreateCoefBlock;

/*{coef.coef.map((item, i) => (
  <div className="painting__coef" key={i}>
    {item}
  </div>))}*/
